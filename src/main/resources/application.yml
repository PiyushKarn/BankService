spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/userDB
    password: Piyush@123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
server:
  port: '7070'